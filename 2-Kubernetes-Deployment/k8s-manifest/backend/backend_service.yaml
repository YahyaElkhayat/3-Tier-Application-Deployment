apiVersion: v1
kind: Service
metadata:
   name: backend-service
   labels:
     app: backend
      
spec:
   type: LoadBalancer
   ports:
     - protocol: TCP
       port: 3500
       targetPort: 3500
   selector:
     app: backend